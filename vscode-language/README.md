# VS Code Language Support

This folder contains a small local VS Code language extension for `.l` and `.H`
files.

To use it during development:

1. Open this folder in VS Code.
2. Press `F5` to launch an Extension Development Host.
3. Open a `.l` or `.H` file in that host.

The extension provides syntax highlighting, indentation hints, and snippets for
common language constructs.
